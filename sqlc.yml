version: "2"

sql:
  - engine: "postgresql"
    queries: "./internal/repository/postgres/query.sql"
    schema: "./db/migrations/"
    gen:
      go:
        package: "postgres"
        out: "./internal/repository/postgres"
        sql_package: "pgx/v5"
        emit_json_tags: true
